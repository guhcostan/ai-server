# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT_ID=marketingdomain-dev-in69
GOOGLE_CLOUD_REGION=us-central1
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Server Configuration
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration (optional)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# ⚠️  AUTOMATIC AUTHENTICATION VIA GCLOUD ⚠️
# This API automatically uses gcloud auth credentials.
# 
# To configure:
# 1. gcloud auth application-default login
# 2. gcloud config set project YOUR_PROJECT_ID
# 3. gcloud services enable aiplatform.googleapis.com
#
# No need to configure service accounts or GOOGLE_APPLICATION_CREDENTIALS! 
# The system will automatically detect your authenticated gcloud credentials. 